# 下一阶段测试计划 (D2 · 无机械臂)

> 前提: D1 已全部真机验证(标定/三态含红背/VLM 认牌 5-5/审计/解说/荷官屏+操作按钮/重开)。
> 原则不变: 错判必须为 0(宁可不确定); 慢循环可以死; 砍单顺序按手册。

## T1 · 完整一手无干预跑通 (最高优先, 上次差三张牌)

荷官按提词发完整手剧情牌, **全程零弹窗零人工**。
```bash
bash tools/run_live.sh        # 荷官屏 http://<SPARK_IP>:8080
```
- [ ] 底牌 8 张一轮一张、两张并排不叠、烧牌进 MUCK、翻转河进 C1~C5
- [ ] 稳定门: 快挥手/慢速伸臂都显示 MOVING(升级后未实测慢速)
- [ ] 手臂悬停挡牌后收回: 不弹任何窗(每步基线快照, 未实测)
- [ ] 结算 ✅, 终局筹码 = [20300, 19900, 19900, 19900](剧情牌标准答案)
- [ ] 解说全程 LLM 生成、事实无误; 会话 jsonl 可回放
**达标线: 连续 2 手零干预跑通。** 出弹窗即记录场景→修→重跑。

## T2 · 前端联调 (对接文档已交付)

- [ ] Spark 起 hub + `run_live.sh --ws`; 大屏浏览器接 `ws://<IP>:8766`
- [ ] 无桌开发路径: `tools/replay.py <session> --ws --speed 4` 重播真实会话
- [ ] index.html 换用 live_adapter.js(映射表见 docs/前端对接-转播大屏.md)
- [ ] 核对: 胜率之河转牌反超剧情、结算高亮、GTO 矩阵 hero 格、审计徽记
- [ ] 断线重连: 刷新页面后一条 game_event 内恢复画面
**验收: 真实一手从发牌到结算, 大屏全要素与桌面实物一致, 延迟 <1.5s。**

## T3 · 识别专项 (让"识别"看得见 + 公开模式)

- [ ] 公开模式整手: `bash tools/run_live.sh --public`(板面全靠 VLM 认, 无预排)
      验收: 5 张公共牌零错判; 人头牌允许走两步问答(慢)或操作员
- [ ] 审计演示: 故意发错一张公共牌 → audit_report mismatch + alert 上大屏
- [ ] bench 复跑(相机现装机位): `python tools/bench_vlm.py --dir <真牌裁图> --url http://localhost:11434/v1`
      记录 P50/P95, 错判必须 0
- [ ] (若第二台 UVC 相机到位) D1-3 模板采集 52 张 → NCC 主读牌 → VLM 降为兜底

## T4 · 故障演练表 (手册 D2-4, 全部真机注入)

| 注入 | 预期 | 状态 |
|---|---|---|
| 荷官发错槽(空格) | 网页确认弹窗, 一键改判或移牌 | 已实战✓(要复测新基线逻辑) |
| 荷官漏发 20s | 轻声重提→升级 alert, 永不卡死 | 已实战✓ |
| 识别不确定 | VLM 仲裁→仍不确定→网页补录 | 组件已验, 需整手内实测 |
| 杀 Ollama(`sudo systemctl stop ollama`) | 解说模板顶上/仲裁跳操作员/审计消失, 零报错 | 组件已验, 需整手内实测 |
| 相机 USB 拔线 | live_cli 报错退出→重插(自动回 webcam)→run_live 重跑, <60s 恢复 | 恢复路径已验 |
| 中途重开 | 网页🔁按钮→自动新一手 | 已实战✓ |
| 前端断连 | 刷新即恢复(T2 一并验) | |

## T5 · 彩排 (D2 收尾)

- [ ] 全链路×3 手连跑: 荷官+2 名"演员"按剧本行动(混入 raise/fold, 非全 check)
- [ ] 解说 TTS 出声(荷官屏🔊或笔记本 tts_client), 音量试场
- [ ] 计时: 一手全程目标 <8 分钟; 记录每个环节耗时找卡点
- [ ] 站位: 上帝屏朝评委; 荷官屏平板放荷官手边
- [ ] 开场 checklist 固化: 预热确认(`systemctl status ollama-warm`)→
      `run_preview.sh` 对位→撤空桌面→`run_live.sh --ws`→大屏连上→开打

## 已知残留风险(明说)

1. 稳定门新逻辑(慢速比对/每步快照)只有单测, **未过真机整手** —— T1 首要目标。
2. 顶视 1080p 认牌是物理上限: 人头牌依赖两步问答(~7s), 公开模式演示节奏要预留。
3. GTO 数据仍是 placeholder 图表(source 字段已标), 正式转录属 D0 数据项。
4. VPIP/PFR/心率大屏元素无数据源(见对接文档), 前端需占位或隐藏。
